# megatron

## Installation

- Modify `config.h` to set `DEFAULT_DIR` and `HISTORY_FILE`

- Create `HISTORY_FILE` run `create_history_file.sh`.

Then run:

```sh
sudo make install
```

## Uninstall

```sh
sudo make remove
```


